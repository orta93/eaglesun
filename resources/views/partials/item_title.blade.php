<div class="container flex flex-col text-left {{ isset($margin) ? $margin : 'my-20' }}">
    <div class="text-xs font-semibold mb-6">{{ $helper }}</div>
    <div class="font-semibold">{{ $type }}</div>
    <div class="text-5xl font-bold">{{ $title }}</div>
</div>
